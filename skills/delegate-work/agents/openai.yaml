interface:
  display_name: "Delegate Work"
  short_description: "Split work across agents safely"
  default_prompt: "Use $delegate-work to split this work into safe parallel tasks and prepare subagent briefs."

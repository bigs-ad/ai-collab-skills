interface:
  display_name: "Plan Work"
  short_description: "Turn requirements into execution plans"
  default_prompt: "Use $plan-work to turn these requirements into an execution plan with dependencies, gates, and verification."
